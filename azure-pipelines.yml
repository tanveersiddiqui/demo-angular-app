# Docker image
# Build a Docker image to deploy, run, or push to a container registry.
# Add steps that use Docker Compose, tag images, push to a registry, run an image, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- master

pool:
  vmImage: 'Ubuntu-16.04'

variables:
  imageName: 'tanveer-demo-angular2:$(build.buildId)'

steps:
- script: docker build -f Dockerfile -t $(imageName) .
  displayName: 'docker build'
  
docker build -t angular12acr.azurecr.io/$(imageName) .
    docker login -u angular12acr -p 0K/9ckDLgYChzjSGrONjTs1z4ET5APnN angular12acr.azurecr.io
    docker push angular12acr.azurecr.io/$(imageName)
    